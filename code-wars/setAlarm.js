//Takes in the booleans of if you are employed and if you are on vacation, and returns true if you need to set you alarm and false if you don't.

let setAlarm = (employed,vacation) => (employed == true && vacation == false) ? true:false